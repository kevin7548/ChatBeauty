# Frontend environment — copy to .env. Vite exposes only VITE_-prefixed vars.

# Backend base URL. Local docker-compose backend listens on :8080; bare uvicorn on :8000.
VITE_API_URL=http://localhost:8080
